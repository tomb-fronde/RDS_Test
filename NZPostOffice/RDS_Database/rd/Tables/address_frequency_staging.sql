CREATE TABLE [rd].[address_frequency_staging] (
    [adr_id]           INT         NOT NULL,
    [sf_key]           INT         NOT NULL,
    [rf_delivery_days] VARCHAR (7) NOT NULL,
    [contract_no]      INT         NOT NULL
);

